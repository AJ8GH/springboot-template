db:
  password: test-password

api:
  root-uri: http://localhost:8080/skeleton
  port: 8080
  path:
    create: /create

spring:
  security:
    user:
      name: test-user
      password: test-password

kafka:
  host-name: localhost
  bootstrap-servers: ${kafka.host-name}:9092
